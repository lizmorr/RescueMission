<% unless @answers == [] || @answers.nil? %>
  <h3 class="subheader">Answers</h3>
  <ul id="all_answers">
    <% @answers.each do |answer| %>
      <li><%= answer.description %></a>
    <% end %>
  </ul>
<% end %>
