    <h1>Revise Question</h1>

    <%= form_for @question do |f| %>
      <h2><%= f.label :title %></h2>
      <%= f.text_field :title %>

      <h2><%= f.label :detail %></h2>
      <%= f.text_area :detail %>

      <%=f.submit "Submit Revision" %>
    <% end %>

  <%= link_to "Actually, just delete this", question_path(@question),
    method: :delete %>
